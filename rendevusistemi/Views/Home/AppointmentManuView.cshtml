@model IEnumerable<rendevusistemi.Database.Data.Appointments>

@{
    ViewBag.Title = "AppointmentManuView";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@

<div class="content-wrapper">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayName("MÜŞTERİ")
            </th>
            <th>
                @Html.DisplayName("OPERASYON")
            </th>
            <th>
                @Html.DisplayName("TARİH")
            </th>
            <th>
                @Html.DisplayName("AÇIKLAMA")
            </th>
            
            
            
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.EmployeId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.JobId)
                </td>
               
                <td>
                    @Html.DisplayFor(modelItem => item.DateTimeStart)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
               
                
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
    </div>
